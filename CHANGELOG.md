# CurriLoop v6.8.2

유사 답안 유예 확장 및 채점 보정판.

- 실제 학습 오답 패턴을 분석해 유사 답안 판정을 규칙 기반으로 확장
- 조사 차이(예: `기술 발전` ↔ `기술의 발전`)를 즉시 오답 대신 유예
- 명백한 한글 오타·짧은 철자 오류를 자모 수준까지 비교해 유예
- `특징/특성`, `구분/구별`, `발전/발달`, `주의/유의`, `활용/사용` 등 의미가 가까운 표현은 유예 후 공식 표현 재확인
- `생활 속/실생활/일상생활`, 일부 활용형 차이, 충분한 부분정답·표현 겹침도 제한적으로 유예
- `설계/구현`, `선택/수집`, `표현/구현`, `분석/구조화`, `문제 해결/문제 분석`처럼 개념·단계가 다른 답은 계속 오답 처리
- 유예 답안은 오답 기록을 만들지 않고 숙련도도 올리지 않음
- 실전 유예 답안은 3문장 뒤 재인출하며, 같은 유사 답안을 두 번 반복하면 즉시 반복을 멈추고 다음 복습에서 다시 확인
- 핵심·정밀에서 유예 뒤 같은 칸을 공식 표현으로 바로 고치면 정확 정답을 별도로 숙련 기록에 반영 가능
- `classifyDetailed()`와 유예 사유(`조사 차이`, `오타 가능`, `유사 표현`, `부분 일치` 등) 추가
- 실제 오답에서 일반화한 대표 사례와 명확한 개념 오답을 함께 검증하는 `tests/grading-engine.test.js` 추가
- v6.8.1의 일일 복습 고정, 브랜드 홈 이동, 장기 복습 간격, 실전 중요도 로직은 유지

개인 학습 백업의 원문 오답 데이터는 배포 파일에 포함하지 않았으며, 일반화된 채점 규칙과 익명화 가능한 대표 표현만 코드·테스트에 반영했습니다.

교육과정 원문 데이터(`curriculum-data.js`, `supplemental-data.js`)는 v6.8.1과 동일합니다.
