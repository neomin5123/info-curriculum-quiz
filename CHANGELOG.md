# CurriLoop v6.9.1

출제 묶음 재구성 및 복습 기록 탐색성 개선판.

- 각론 `출제 항목` 드롭다운을 학습 중요도와 연관성에 맞게 재구성
  - `내용체계 + 성취기준`: 내용 체계와 해당 영역의 성취기준 본문을 한 흐름으로 학습
  - `해설 + 적용 시 고려사항`: 성취기준 해설과 적용 시 고려사항을 별도 묶음으로 학습
  - `성격·목표`, `교수학습·평가`, `전체`는 유지
  - 학습 기록의 내부 출처 키(`content-system`, `achievement`)는 그대로 유지하여 기존 복습·백업과 호환
  - v6.9.0에서 `내용 체계` 또는 `성취기준`을 선택해 둔 UI 상태는 `내용체계 + 성취기준`으로 자동 복원
  - 오답 기록의 `원문에서 보기`는 해당 기록이 성취기준 본문인지 해설/고려사항인지 판별해 새 묶음으로 이동
- 복습 `기록 보기`에 정렬 드롭다운 추가
  - 취약순(기본)
  - 오답 많은 순
  - 최근 학습 순
  - 오래된 학습 순
  - 최근/오래된 학습 순은 숙련 기록의 `lastSeenAt`을 우선 사용하고, 없으면 오답 시각으로 보완
- 기존 개별 `삭제` 버튼은 그대로 유지
- 학습 여부를 임의 추론하는 보호 기능은 추가하지 않음
- 가상 출제 묶음 도입에 맞춰 현재 화면 초기화와 점수 집계를 보이는 입력 기준으로 정밀화
- 백업 스키마 v8 및 기존 학습 데이터 호환성 유지

교육과정 원문 데이터(`curriculum-data.js`, `supplemental-data.js`)는 v6.9.0과 바이트 단위로 동일합니다.
