# CurriLoop v6.9.0

채점·복습 정제 및 기출형 전이 연습 보강판.

- 유사 답안 유예를 임용 기준에 맞게 재조정
  - 조사·띄어쓰기·명백한 오타·일부 표현 차이는 계속 유예
  - `탐색/발견`, `설계/구현`, `선택/수집`, `분석/구조화`, `처리/해결` 등 핵심 행동어 혼동은 즉시 오답
  - 단어 일부가 겹친다는 이유만으로 `구성되는 컴퓨팅 시스템` ↔ `동작하는 컴퓨팅 시스템` 같은 문장을 유예하지 않음
- 유사 답안 결과에 `오답으로 처리` 추가
  - 현재 유예 기록을 오답으로 전환하고 같은 표현쌍은 이후 바로 오답 판정
  - 개인 채점 보정은 브라우저에 저장하고 v8 백업에 포함
- `모름`을 오답과 분리
  - 빈 답 제출은 `unknown`으로 기록
  - 장기 숙련 연속 성공은 끊고 복습 시점은 당기되, 오답 표현 이력과 `wrongCount`에는 넣지 않음
  - 실전 재인출·세트 요약·백업 통계에서도 별도 집계
- 복습 화면에 `다음 복습: 내일 · N개` 형식의 예정 정보 추가
- 실전 세트 완료 요약 추가
  - 정확 / 유예 / 오답 / 모름 / 재인출 회복을 한 줄로 표시
- 기출형 미니 체크를 12% 랜덤에서 `실전 3세트 완료마다 1문제 보장`으로 변경
  - 이미 실제로 학습한 범위에서만 출제
  - 범주 판별 → 영역 판별 → 실제 오답 교정 유형을 가능한 범위에서 순환
  - 재인출이 겹치면 재인출을 먼저 끝낸 뒤 미니 체크를 제시
- 성취기준 계열 실전 강도 차등화
  - 성취기준은 기존 정확 인출 강도 유지
  - 성취기준 해설은 초기 1개 중심, 간격 성공이 쌓이면 최대 2개
  - 성취기준 적용 시 고려 사항은 한 문장당 핵심 단서 1개 중심
  - 교수학습·평가 방법, 연계, 학습자 수준 등 식별력이 높은 표현을 우선
- 학습 로직 추가 모듈화
  - `js/review-engine.js`: 다음 복습 버킷·실전 세트 요약
  - `js/history-engine.js`: 오답 횟수·날짜·정렬·취약 판정
  - `js/storage-engine.js`: 백업 스키마 v8 및 payload 생성
- 백업 스키마 v8
  - `unknownCount`, 실전 `unknown`, 개인 채점 보정, 기출 미니 체크 누적 상태 저장
  - 기존 v1~v7 백업은 계속 가져오기 가능
- 회귀 테스트 확대: 채점 엄격화, 모름 분리, 섹션별 실전 강도, 새 모듈·백업 v8 검증

교육과정 원문 데이터(`curriculum-data.js`, `supplemental-data.js`)는 v6.8.2와 바이트 단위로 동일합니다.
