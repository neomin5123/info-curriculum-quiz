# CurriLoop v6.0.0 공개판 상세 검수 보고서

- 검수일: 2026-09-12
- 기준판: `CurriLoop_v6_Public_RC1_FINAL.zip`
- 기준판 SHA-256: `1eb3c5bde53827ce59961db12a5705bce14fbaa7388d6667d5e0d04ce25ed23a`
- 수정 원칙: 기준 ZIP의 기능·데이터 구조를 보존하면서 학습 신뢰성, 장기 오답 호환, 공개 배포 품질에 직접 관련된 부분만 수정

## 1. 핵심 결론

이번 작업은 기존 RC1을 재작성하지 않고 **source of truth로 고정한 뒤 결함·미완성 부분을 보강**했다. RC1에는 이미 550개 명시적 stable ID, 랜덤 shuffle-bag, PWA 업데이트 UI, `hard` 호환 일부 등 이전 개선이 들어가 있었으므로 이를 유지했다. 추가로 전체 채점의 미입력 누락 버그, 장기 오답 이력 구조, 백업 검증, 원문 변경 시 오답 연결, 중학교 정보 수동 빈칸, PWA 캐시 설치 정책, 접근성·안내 문구를 보강했다.

## 2. 요청 사항별 처리

| 요청 | 처리 결과 |
|---|---|
| 핵심 / 보통 / 야~호! → 핵심 / 정밀 / 야~호! | 완료. 선택창뿐 아니라 `difficultyLabel`, 오답 이력 마이그레이션, 복습 재진입, 안내 문구를 점검. 내부 `easy/normal`은 호환을 위해 유지. 사용자 화면에 쉬움/보통/어려움 잔존 0건. |
| 빈칸 Enter/모바일 다음: 미입력도 오답 | 완료. 개별 Enter는 빈값을 오답 처리하고 다음 빈칸으로 이동. **전체 채점도 빈 입력을 모두 오답 처리하도록 수정**. |
| 진짜 stable ID | RC1에 이미 550/550 명시적 ID가 존재함을 확인하고 그대로 유지. ID를 원문 문자열에서 재생성하지 않음. 오답 재연결에서 같은 stable line ID를 우선 사용하도록 강화. 중복 0건. |
| 원문 수정 후 과거 오답 연결 | 정확한 `lineId + answer + occurrence` 우선, 빈칸 문구가 바뀌면 같은 `lineId`의 첫 빈칸으로 후퇴, 그 다음에만 구형 위치 기반 fallback 사용. |
| 랜덤 단원 연속 진행 | 기존 shuffle-bag 구조를 유지·검증. 마지막 빈칸 채점 후 다음 랜덤 단원으로 자동 진행하는 동적 테스트 통과. |
| 구형 hard 기록 | `hard`, `어려움`, 구형 `보통` 등 저장값을 `normal/정밀`로 마이그레이션. 복습 진입도 hard→normal fallback 유지. |
| 백업 가져오기 검증 | schema v4, history/mastery 구조·형식·개수 검증, 잘못된 항목의 조용한 필터링 금지, 사전 스냅샷 저장 실패 시 중단, 쓰기 후 read-back 검증, 후속 파싱 검증, 실패 시 롤백, FileReader 오류 처리. |
| 미해결 오답 vs 과거 오답 이력 | `미해결`은 현재 복습 대상, `전체 항목(해결 포함)`은 과거 포함이라는 UI 의미를 명시. 개념별 누적 오답 횟수와 최초/최근 오답을 분리 표시. |
| 세션 점수 의미 | 화면 지표를 **세션 시도**로 명시하고 장기 숙달 점수로 오해하지 않도록 도움말 보강. |
| 원문 보기 vs 단원 초기화 | 원문 보기는 현재 문장을 그대로 보여 주는 보기 전환, 초기화는 현재 단원의 입력/채점 상태 초기화로 분리된 상태를 유지하고 동적 검증. |
| 오답 장기 보존 | 개념 항목 자체는 임의 개수 제한 없음. `attempts`, `firstWrongAt`, `lastWrongAt` 유지 + 최근 100회 상세 이벤트와 `archivedWrongEvents` 누적 카운터. |
| PWA 캐시/업데이트 | 캐시 버전 갱신. 핵심 precache 실패 시 새 SW 설치를 실패시켜 불완전한 오프라인판 활성화를 막고, 부가 파일은 실패해도 설치를 막지 않음. HTML은 network-first, 정적 자산은 cache-first. 사용자 업데이트 수락 시에만 `SKIP_WAITING`. |
| 모바일/노트북 CSS | 위험한 전면 재작성은 하지 않고 중복 선언 1건 제거, 390px/1366px에서 수평 overflow 없는지 동적 검사. |
| 접근성 | `:focus-visible`, 모바일 설정 `aria-expanded/aria-controls`, 도움말 모달 focus restore·Tab trap·Escape close, 숨은 백업 파일 입력 aria-label 보강. |
| 공식 출처 표시 | `원문 출처` 성격을 `공식 출처`로 명료화하고, 중학교 정보는 현재 영역에 해당하는 공식 인쇄 페이지 범위를 버튼에 표시. |
| 자동 데이터 검증 | `tools/validate_curriloop.py` 추가. 배포 전 요청된 정적 검사를 한 번에 수행. |
| dead code | 더 이상 사용하지 않는 hard/자동 형태소 기반 빈칸 생성 함수·상수 제거. `hard` 문자열은 과거 데이터 마이그레이션을 위해 필요한 부분만 유지. |
| 공개 UI | 오답 필터, 세션 지표, 출처 버튼, 도움말 문구를 타인이 처음 보더라도 의미를 파악할 수 있도록 정리. 불필요한 새 기능은 추가하지 않음. |

## 3. 중학교 정보 공식 원문 전수 검수

공식 기준은 교육부 고시 제2022-33호 [별책10] 공통 교육과정 「정보」 인쇄 p.14–21(PDF p.20–27)이다. 5개 영역에서 핵심 아이디어, 지식·이해, 과정·기능, 가치·태도, 성취기준, 성취기준 해설, 성취기준 적용 시 고려 사항을 모두 대조했다.

**결과:** RC1의 중학교 정보 111개 `text`는 공식 원문과 대응되는 정리본으로 이미 정확해, 이번 작업에서 본문을 임의로 다시 쓰지 않았다. 공식 PDF 파싱 결과의 `컴퓨 , 팅`, `분, 석`, `교수 학습 ⋅` 같은 추출 잡음은 앱 원문에 넣지 않았다. 누락·중복·용어 불일치로 판정된 항목은 0건이다. 별도의 `tools/middle_info_official_snapshot.json`을 만들어 이후 변경을 source-lock으로 검출한다.

| 영역 | 문장 | 내용 체계 | 성취기준·해설·고려 | 수동 빈칸 지정 변경 문장 | 핵심 실제 빈칸 | 정밀 실제 빈칸 |
|---|---:|---:|---:|---:|---:|---:|
| 컴퓨팅 시스템 | 19 | 10 | 9 | 16 | 93 | 122 |
| 데이터 | 22 | 11 | 11 | 19 | 130 | 153 |
| 알고리즘과 프로그래밍 | 33 | 17 | 16 | 26 | 172 | 196 |
| 인공지능 | 21 | 10 | 11 | 19 | 121 | 142 |
| 디지털 문화 | 16 | 10 | 6 | 12 | 93 | 103 |

합계 **111문장**, RC1 대비 빈칸 지정 변경 **92문장**, 핵심 **609개**, 정밀 **716개**의 실제 빈칸이 생성된다. 문장 수가 아니라 실제 겹침 제거 후 렌더되는 빈칸 기준이다.

## 4. 중학교 정보 핵심/정밀 빈칸 편집

111개 문장을 한 줄씩 검토했다. 자동 형태소 규칙을 적용하지 않았고 `line.easy`와 `line.normal`의 명시적 수동 목록만 사용한다. RC1 대비 핵심 목록은 90문장, 정밀 목록은 83문장에서 실제 수정됐으며, 둘 중 하나 이상이 바뀐 문장은 92문장이다. 변경하지 않은 문장도 동일 기준으로 재검토했다.

편집 기준은 다음과 같다: (a) 지식·이해의 핵심 명칭은 핵심 난이도에서 노출시키지 않음, (b) 과정·기능의 행위와 대상이 출제 포인트이면 둘 다 회상하도록 가림, (c) 정밀은 조사·어미와 최소 골격을 남기되 핵심보다 넓게 가림, (d) 하나의 교육과정 개념은 의미 단위를 유지, (e) 같은 답이 반복되는 경우 렌더링 로직상 불필요한 중복 빈칸을 억제.

## 5. 채점·정규화

- 빈 입력은 `gradeOne()`에서 오답으로 기록되며 Enter/모바일 다음 입력으로 다음 빈칸에 진행한다.
- `gradeAllVisible()`은 더 이상 `value.trim()`이 있는 입력만 고르지 않고 **현재 단원의 모든 빈칸**을 채점한다.
- 정답 비교 정규화는 Unicode NFKC 후 공백 및 일반 문장부호를 제거한다. 따라서 `기술·가정/정보 교과(군)`과 `기술가정/정보 교과군`처럼 가운데점·괄호·슬래시 차이는 채점에 영향을 주지 않는다.

## 6. 데이터 호환성과 마이그레이션

- 내부 난이도 키 `easy/normal/yaho`는 그대로 유지해 기존 localStorage와 호환한다.
- 구형 `hard`는 읽을 때 `normal/정밀`로 정규화한다. `쉬움/보통/어려움` 라벨도 저장 이력에서만 해석하고 새 UI에는 노출하지 않는다.
- stable ID가 유지되므로 단순 문구 수정에도 과거 오답은 같은 교육과정 문장으로 연결될 수 있다. 빈칸 정답 문구가 바뀌어 exact match가 실패해도 같은 line ID 내부로 fallback한다.
- 오답 개념 항목 수를 임의로 잘라내지 않는다. 상세 이벤트는 최근 100회만 저장하고 그 이전 횟수는 `archivedWrongEvents`로 누적하여 저장 용량을 통제한다.

## 7. 자동 검사 결과

`python tools/validate_curriloop.py` 최종 결과: **PASS**

```text
CurriLoop validation: PASS
- subjects: 6
- areas: 27
- curriculum lines: 550
- general IDs: 28
- middle-info source lock: 111/111
- zero core gaps: 0
- zero precision gaps: 0
- missing manual gap terms: 0
- precision<core: 0
- duplicate stable IDs: 0
```

이 검사에는 **인라인 JS 문법, Service Worker 문법, 6과목, 27영역, 550문장, 중복 stable ID, 핵심/정밀 0빈칸, 원문에 없는 수동 빈칸, 정밀<핵심 이상 사례, 총론 ID 중복, 사용자 화면 옛 난이도 명칭, 자동 빈칸 dead code, 중학교 정보 111 source-lock, manifest/cache identity**가 포함된다.

추가로 Playwright 기반 동적 smoke test **27/27 PASS**: 빈 Enter 오답·포커스 이동, 전체 채점 미입력, 랜덤 단원 자동 연속 진행, hard 기록·legacy stable ID 마이그레이션, 오답노트 정밀 표시, 백업 record validator, 모바일 ARIA, 390px/1366px overflow, 도움말 modal, 런타임 JS 오류 등을 검사했다.

### PWA 런타임 검증의 한계

현재 실행 샌드박스는 localhost/file origin 브라우저 탐색을 차단하므로 실제 Service Worker 설치·교체를 브라우저 자동화로 끝까지 재현할 수는 없었다. 대신 Service Worker 문법 검사와 캐시 정책의 정적 검토를 수행했고, 앱 UI 동적 검사는 동일 HTML을 Playwright에 주입하고 테스트 전용 in-memory Storage를 사용했다. 따라서 **실제 Vercel 배포 후 한 번은 설치된 PWA에서 업데이트 알림 → 새로고침 → 오프라인 재실행을 실기기로 확인하는 것이 권장된다.**

## 8. 의도적으로 건드리지 않은 부분

- 야~호!의 문장 단위 완전 회상 로직은 목적이 명확하고 이상이 없어 유지했다.
- 6과목/27영역/약 550문장 전체 UI 구조와 기존 총론/각론/오답노트 기능을 유지했다.
- CSS를 새 프레임워크로 갈아엎거나 학습과 직접 관계없는 기능을 추가하지 않았다.
- 중학교 정보 공식 본문이 이미 맞는 곳은 “수정했다”는 이유만으로 문구를 다시 쓰지 않았다.

## 9. 배포

최종 ZIP의 내용은 GitHub 저장소 **루트에 그대로 덮어쓰기** 하면 된다. `index.html`, `service-worker.js`, `manifest.webmanifest`, `icons/`, `robots.txt`, `sitemap.xml`을 함께 배포해야 한다. 배포 뒤 기존 PWA 사용자는 업데이트 알림을 수락하거나 앱을 다시 열어 새 Service Worker를 활성화한다.
